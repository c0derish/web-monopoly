package webmonopoly.backend.model;

public class ActionResponse {

    boolean done;

    public ActionResponse(boolean done) { this.done = done; }

    public boolean getDone() { return this.done; }

    public void setDone(boolean done) { this.done = done; }
}
